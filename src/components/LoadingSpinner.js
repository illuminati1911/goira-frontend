import React from 'react'
import spinnerStyles from './layout/styles/LoadingSpinner.module.css'

const LoadingSpinner = () => {
    return (
            <div className={spinnerStyles.ldsdualring} />
    )
}

export default LoadingSpinner